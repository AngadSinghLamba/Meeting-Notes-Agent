from typing import Tuple

def ocr_extract_text(image_bytes: bytes) -> Tuple[str, float]:
    """
    MVP2 stub.
    Later: call Azure AI Vision / OCR engine here.
    For now: return empty text + 0 confidence.
    """
    return "", 0.0
